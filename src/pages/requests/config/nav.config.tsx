const breadcrumbs = {
  label: "Solicitudes en trámite",
  url: "/",
};
const RequestsNav: Record<string, { path: string }> = {
  Vinculación: { path: "/requests" },
};

export { breadcrumbs, RequestsNav };
